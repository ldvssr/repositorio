#
import random
palavras = ["autocarro", "automovel", "aviao", "camiao", "motociclo", "ciclomotor", "velocipede", "triciclo"]
palavra = random.choice(palavras)
print (palavra)
'''
#
while correctos < len(palavra) and tentativas > 0:

	# aqui fica o código para o jogador inserir as letras, uma de cada vez.
#
input(‘Insira uma letra’)
#
for n in range(0, len(palavra)):
	if palavra[n] == letra inserida:
		a letra existe
	else:
		a letra não existe
#
adivinha = []
for n in range(0, len(palavra)):
    adivinha.append("_")
#
'''