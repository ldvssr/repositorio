#include <stdio.h>

main()

{
    float ordem;
    float peso;
    
    printf ("qual o peso:\n");
    scanf ("%f",&peso);
    printf ("qual a ordem:\n");
    scanf ("%f",&ordem);
    
    
    if 
        (peso<50)
        printf ("nao pode viajar\n");
    else
        if
            (peso<0)
            printf ("peso invalido, qual o peso:\n");
            scanf ("%f",&peso);
        else
            if
            (ordem<0)
            printf ("ordem invalida, qual a ordem:\n");
            scanf ("%f",&peso);
            else
                if
                (ordem==1)
                printf ("em mercurio o peso sera %f\n",peso*0.38=total,total)
                else
                    if
                    (ordem==2)
                printf ("em venus o peso sera %f\n",peso*0.90=total)
                    else
                        if
                        (ordem==3)
                printf ("aqui o peso sera %f\n",peso)
                        else
                            if
                            (ordem==4)
                printf ("em marte o peso sera %f\n",peso*0.39=total)
                            else
    
}